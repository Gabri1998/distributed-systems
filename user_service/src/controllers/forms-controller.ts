import FormSchema from "../schemas/forms";
import { MessageException } from "../exceptions/MessageException";
import { MessageHandler, MessageData } from "../utilities/types-utils";

const calculateScore: MessageHandler = async (data) => {
    

};
